import pandas as pd
import random

# Configuración
num_registros = 500
canales = ["web", "mobile", "ATM"]

# Generar registros simulados
datos = []
for i in range(1, num_registros + 1):
    user_id = f"user_{i}"
    amount = round(random.uniform(50, 100000), 2)
    hour = random.randint(0, 23)
    channel = random.choice(canales)
    is_fraud = random.choices([0, 1], weights=[0.9, 0.1])[0]  # 10% fraude

    datos.append({
        "user_id": user_id,
        "amount": amount,
        "hour": hour,
        "channel": channel,
        "is_fraud": is_fraud
    })

# Crear DataFrame y guardar como CSV
df = pd.DataFrame(datos)
df.to_csv("users_transactions.csv", index=False)
